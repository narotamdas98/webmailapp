user = webmailappuser
password = my_password
hosts = 172.18.0.4
dbname = webmailappdb
query = SELECT split_part(email, '@', 2) || '/' || split_part(email, '@', 1) || '/Maildir/' FROM "User" WHERE email = '%s'
